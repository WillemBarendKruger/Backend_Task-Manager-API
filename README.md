A typescript based API.
A simple backend Task Management API for a task management platfrom.

NOTE: This is still being worked on to improve and add features, so there is a few comments here and there

To get neccesery modules run:
npm install

TO run the server:
npm run start
